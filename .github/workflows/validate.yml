name: Validate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  yaml-lint:
    name: YAML Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install yamllint
        run: pip install yamllint
      - name: Lint YAML files
        run: yamllint -d relaxed .

  markdown-lint:
    name: Markdown Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DavidAnson/markdownlint-cli2-action@v19

  shell-lint:
    name: Shell Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run ShellCheck
        run: shellcheck scripts/*.sh

  compose-validate:
    name: Compose Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create required files for validation
        run: |
          mkdir -p secrets
          echo -n "placeholder" > secrets/db_password.txt
          echo -n "placeholder" > secrets/grafana_admin_password.txt
          cp .env.example .env

      - name: Validate main compose file
        run: docker compose config --quiet

      - name: Validate monitoring compose file
        run: docker compose -f monitoring/docker-compose.yml config --quiet

  line-length:
    name: Giant-Line Regression
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check for lines exceeding 500 characters
        run: |
          echo "Checking for lines longer than 500 characters..."
          status=0
          while IFS= read -r -d '' file; do
            result=$(awk 'length > 500 {printf "  line %d: %d chars\n", NR, length}' "$file")
            if [ -n "$result" ]; then
              echo "FAIL: $file"
              echo "$result"
              status=1
            fi
          done < <(find . -type f \( -name '*.md' -o -name '*.yml' -o -name '*.yaml' -o -name '*.sh' \) ! -path './.git/*' -print0)
          if [ "$status" -eq 0 ]; then
            echo "PASS: No lines exceed 500 characters."
          fi
          exit "$status"

  link-check:
    name: Link Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: lycheeverse/lychee-action@v2
        with:
          args: --no-progress --offline '**/*.md'
          fail: true
