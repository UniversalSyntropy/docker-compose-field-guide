# =============================================================================
# ENVIRONMENT VARIABLES — TEMPLATE
# =============================================================================
# Copy this file to .env and edit with your values:
#   cp .env.example .env
#
# Docker Compose loads .env automatically from the same directory as the
# compose file. Do NOT commit .env to git — add it to .gitignore.
# =============================================================================

# Timezone — IANA format (https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=UTC

# User/group IDs for file ownership inside containers
# Run `id` to find your UID/GID (usually 1000 on single-user Linux systems)
PUID=1000
PGID=1000

# Root directory for all persistent Docker volumes
DATA_DIR=/mnt/data

# Reverse proxy (used by recipes/traefik.yml)
# Your domain name — Traefik uses this for routing and certificate requests
DOMAIN=example.com
# Email for Let's Encrypt certificate notifications
ACME_EMAIL=admin@example.com
