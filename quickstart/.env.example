# =============================================================================
# QUICKSTART — ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and optionally edit:
#   cp .env.example .env
#
# The defaults work out of the box — you only need to change values if
# you have port conflicts or prefer different paths.
# =============================================================================

# Timezone — IANA format (https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=UTC

# User/group IDs — used by Homepage for file ownership
# Run `id` to find your UID/GID (usually 1000 on single-user systems)
PUID=1000
PGID=1000

# Root directory for persistent data (Uptime Kuma's SQLite database)
# Default: ./data (relative to this directory)
DATA_DIR=./data

# Port mappings — change if you have conflicts on 3000 or 3001
HOMEPAGE_PORT=3000
UPTIME_KUMA_PORT=3001
